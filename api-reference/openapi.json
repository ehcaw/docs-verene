{
  "openapi": "3.1.0",
  "info": {
    "title": "Pill Test API",
    "version": "1.0.0",
    "description": "Minimal spec for testing ParamField post/pre pill derivation."
  },
  "servers": [
    {
      "url": "https://api.example.com"
    }
  ],
  "paths": {
    "/users/{userId}": {
      "get": {
        "summary": "Get a user",
        "operationId": "getUser",
        "parameters": [
          {
            "name": "userId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "x-mint": {
                "post": ["uuid", "name", "date of birth"]
              }
            },
            "description": "The ID of the user to fetch."
          }
        ],
        "responses": {
          "200": {
            "description": "User object",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          }
        }
      }
    },
    "/users": {
      "post": {
        "summary": "Create a user",
        "operationId": "createUser",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateUserRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The created user",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "User": {
        "type": "object",
        "required": ["id", "email", "createdAt"],
        "properties": {
          "id": {
            "type": "string",
            "readOnly": true,
            "description": "Server-generated identifier.",
            "x-mint": {
              "post": ["uuid"]
            }
          },
          "email": {
            "type": "string",
            "format": "email",
            "description": "The user's email address.",
            "x-mint": {
              "post": ["PII"]
            }
          },
          "plan": {
            "type": "string",
            "enum": ["free", "pro", "enterprise"],
            "description": "Billing plan tier.",
            "x-mint": {
              "pre": ["billing"],
              "post": ["beta"]
            }
          },
          "createdAt": {
            "type": "string",
            "format": "date-time",
            "readOnly": true,
            "description": "Timestamp when the user was created."
          },
          "internalNotes": {
            "type": "string",
            "readOnly": true,
            "description": "Notes visible only in the dashboard.",
            "x-sensitivity": "internal-only",
            "x-mint": {
              "post": ["internal"]
            }
          },
          "betaFlag": {
            "type": "boolean",
            "description": "Tests boolean-true config rule (pill = key name).",
            "x-beta": true
          },
          "disabledFlag": {
            "type": "boolean",
            "description": "Tests boolean-false config rule (no pill).",
            "x-beta": false
          },
          "priority": {
            "type": "integer",
            "description": "Tests number config rule.",
            "x-priority": 3
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Tests array-of-strings config rule (one pill per element).",
            "x-tags": ["internal", "preview", "deprecated-soon"]
          },
          "opaqueConfig": {
            "type": "string",
            "description": "Tests object config rule (skipped).",
            "x-config": { "nested": "value" }
          }
        }
      },
      "CreateUserRequest": {
        "type": "object",
        "required": ["email", "password"],
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "description": "The user's email address.",
            "x-mint": {
              "post": ["PII"]
            }
          },
          "password": {
            "type": "string",
            "writeOnly": true,
            "minLength": 12,
            "description": "Password, never returned in responses.",
            "x-sensitivity": "secret",
            "x-mint": {
              "post": ["sensitive"]
            }
          },
          "plan": {
            "type": "string",
            "enum": ["free", "pro", "enterprise"],
            "description": "Billing plan tier.",
            "x-mint": {
              "pre": ["billing"],
              "post": ["beta"]
            }
          }
        }
      }
    }
  }
}
